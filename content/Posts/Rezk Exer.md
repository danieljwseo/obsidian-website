---
title:
created:
tags:
draft: true
---
2.7: trivial, naturality is key to show subcomplex
2.9: bijection forward is simple, backwards is you choose the terminal map and that decides every other map 
2.10: comes easily from 2.9
2.11: just take the mapping under the terminal map, want to know it's an injection, note has a retraction or left inverse, so must be injective 
0 -> n -> 0
X_0 <- X_n <- X_0
3.2: Yoneda 
3.3: again obvious by Yoneda 
3.6: forward obvious, reverse obvious by X being a functor, no maps into empty set 
3.8: injective is easy by inclusion being injective itself, surjective, you take preimage and you know it preserves order, so can construct a value such that it maps to wtva 
4.6: take the discrete category with objects S
4.8: forward is again trivial, reverse you have by naturality any map X_1 -> X_n, correspond them to all possible edges, i.e. <01>, <12>, ..., <n-1 n>, coincides on all, so coincides on all [n], proving. 
6.2: simple verifying colimit exericse
6.4: you should get it's isomorphic to 1/2Z 
6.5: should just be N
6.6: just check 
6.7: basically follows by definition
6.8: surjective is obvious, injective to show t=t' implies we use the restrictions on cal{A}, to show reverse we need 6.7

